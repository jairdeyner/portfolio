import styles from "./Chip.module.scss";

const Chip = () => {
  return (
    <div className={styles.chip}>
      <p className={styles.chip__text}>Desarrollador Front-End</p>
    </div>
  );
};

export default Chip;
