import styles from "./Chip.module.scss";

const Chip = () => {
  return (
    <div className={styles.chip}>
      <p className={styles.chip__text}>Desarollador Web</p>
    </div>
  );
};

export default Chip;
